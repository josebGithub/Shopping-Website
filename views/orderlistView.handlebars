     <div>
        <header>
           <h1>Order List</h1>
        </header>
        <hr/>
     </div>
        <div>
            <div>
               <table align="center">
                    <thead>
                    <tr>
                         <th>Order Date</th>
                          <th>Order ID</th>
                    </tr>
                    </thead>
                   <tbody>
                    {{#each data}}
                    <tr>
                       <td>{{this.date}}</td>
                       <td> <a href="/home/orders/{{this.orderId}}">{{this.orderId}}</a></td>
                    </tr>
                    {{/each}}
                    </tbody>
                </table>
            </div>
        </div>

           